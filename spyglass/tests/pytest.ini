[pytest]
addopts = -ra --strict-markers
markers =
    fast: runs with numpy only, no GPU / torch / ldpc / ortools
    gpu: requires torch with a CUDA-capable device
    ldpc: requires the ldpc package
